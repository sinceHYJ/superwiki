interface:
  display_name: "SuperWiki 客户端发布"
  short_description: "同步版本、提交并推送标签，由 GitHub Actions 构建客户端"
  default_prompt: "使用 $superwiki-release 对比上个版本后的提交，升级版本、提交并推送发布标签，由 GitHub Actions 构建 SuperWiki 客户端。"

policy:
  allow_implicit_invocation: true
